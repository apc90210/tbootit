# Техническая документация: Stage 07E-R1 — Восстановление нового сервера и Disaster Recovery

## 1. Назначение и цели этапа

**Цель:** Доказать и гарантировать детерминированное восстановление работоспособного контура ТехноРебут на чистом сервере/VM с Debian 12 на основе только исходного кода из Git и одного архива резервной копии ТехноРебут (`TECHNOREBOOT_BACKUP_*.zip`), без какой-либо зависимости от старого работающего сервера.

### Ключевые гарантии этапа:
1. **Полная независимость:** Восстановление не требует доступа к старому серверу или работающему веб-интерфейсу.
2. **Непрерывность доверия PKI (Auth Continuity):** Восстановленный корневой удостоверяющий центр (CA) признаёт ранее выданный сертификат Владельца (`OWNER`), исключая необходимость перегенерации CA или перевыпуска сертификатов.
3. **Безопасность восстановления (Security & Safe Staging):** Защита от уязвимости Zip Slip (блокировка `../` и абсолютных путей), строгая предварительная проверка манифеста и схемы, двухэтапная распаковка во временную staging-директорию с валидацией SQLite БД (`PRAGMA integrity_check`).
4. **Сохранение стабильности файловых дескрипторов Docker:** Пофайловая синхронизация (`_safe_sync_dir`) для предотвращения разрыва Docker bind-mounts.
5. **Чёткое разделение двух политик восстановления:**
   - *Политика A (Обычное восстановление через веб `/backups`):* Восстанавливает только бизнес-данные (БД, медиа, Авито), **сохраняя текущие активные сертификаты `data/auth`**, чтобы не разрывать сессию Владельца и не откатывать недавно созданные сертификаты.
   - *Политика B (Аварийное восстановление чистого сервера):* Восстанавливает всю систему целиком, **включая `data/auth`** из бэкапа, оживляя исторический CA и сертификат Владельца.

---

## 2. Архитектура и компоненты

### Точки входа:
- `scripts/bootstrap_restore.sh` — POSIX Bash-скрипт предварительной проверки ОС (Debian/Ubuntu), зависимостей (Python 3, cryptography, Docker) и запуска движка восстановления.
- `scripts/bootstrap_restore.py` — Кроссплатформенный детерминированный движок аварийного восстановления, реализующий валидацию архива, защиту от path traversal, staging, синхронизацию каталогов, управление docker compose и верификацию данных.

### Структура восстанавливаемых компонентов:
| Компонент | Источник в архиве | Целевой путь на хосте | Назначение |
| :--- | :--- | :--- | :--- |
| **База данных** | `database/technoreboot.db` (или `.sql`) | `data/db/technoreboot.db` | SQLite база: 23 таблицы, товары, продажи, ремонты, аудит |
| **Медиафайлы** | `storage/product_photos/` | `data/storage/product_photos/` | Оригиналы и превью фотографий товаров |
| **Auth PKI** | `auth/ca/`, `auth/certificates/`, `auth/server/`, `auth/registry.json` | `data/auth/` | Корневой CA, сертификат Владельца, реестр сертификатов, ключи сервера |
| **Авито-модуль** | `avito-module/` | `data/avito-module/` | Мутабельное состояние модуля Авито |
| **Конфигурация** | `config/.env` | `.env` | Переменные окружения (если отсутствуют на хосте) |

---

## 3. Политики безопасности и непрерывности PKI

### Предотвращение Zip Slip:
При чтении любого файла из ZIP-архива проверяются все элементы пути (`namelist`):
```python
for name in namelist:
    if name.startswith("/") or name.startswith("\\"):
        raise SecurityError("Absolute path forbidden")
    if ".." in Path(name).parts:
        raise SecurityError("Directory traversal forbidden")
```
При обнаружении любого подозрительного файла операция прерывается до создания или изменения файлов на диске.

### Непрерывность доверия сертификата Владельца:
1. Восстанавливаются файлы `auth/ca/ca.crt` и `auth/ca/ca.key`.
2. Восстанавливаются `auth/certificates/owner.crt`, `owner.key`, `owner.p12`.
3. Восстанавливается `auth/registry.json` со статусом `ACTIVE` для Владельца и сохранением статусов отозванных сертификатов (`REVOKED`).
4. Менеджер авторизации (`AuthManager`) при старте видит существующие валидные сертификаты CA и Владельца и **ни в коем случае не генерирует новый CA**.
5. Шлюз `gateway` (Nginx mTLS на порту 8443) проверяет входящий клиентский сертификат против `auth/ca/ca.crt` и внутреннего эндпоинта `admin-shell:8010/internal-auth/verify`. Запросы с сертификатом Владельца успешно проходят аутентификацию (HTTP 200), а запросы без сертификата отклоняются (HTTP 403).

---

## 4. Результаты тестирования (Набор тестов A - Z)

В файле `tests/test_stage07e_r1_disaster_recovery.py` реализована автоматизированная верификация всех 26 требований (A - Z):

| Тест | Описание | Результат |
| :--- | :--- | :--- |
| **A** | Валидный архив резервной копии успешно принимается | **PASS** |
| **B** | Архив без `manifest.json` строго отклоняется | **PASS** |
| **C** | Неподдерживаемая версия формата бэкапа отклоняется | **PASS** |
| **D** | Попытка path traversal (`../evil.txt`, `/abs`) блокируется | **PASS** |
| **E** | Архив без базы данных отклоняется | **PASS** |
| **F** | Неполный архив без auth-файлов для нового сервера отклоняется | **PASS** |
| **G** | Поэтапная распаковка (staging) завершается успешно | **PASS** |
| **H** | Корневой CA и сертификат Владельца из бэкапа восстановлены | **PASS** |
| **I** | Существующий клиентский сертификат Владельца признаётся системой | **PASS** |
| **J** | Закрытые разделы Владельца (`/backups`, `/certificates`) доступны | **PASS** |
| **K** | Запросы без сертификата отклоняются шлюзом (403) | **PASS** |
| **L** | Количество товаров совпадает с манифестом (227) | **PASS** |
| **M** | Количество товаров со связью Авито совпадает (154) | **PASS** |
| **N** | Количество локальных товаров совпадает (73) | **PASS** |
| **O** | Количество продаж совпадает с манифестом (50) | **PASS** |
| **P** | Количество строк фотографий в БД совпадает (490) | **PASS** |
| **Q** | Файлы медиа хранилища возвращают HTTP 200 (образцы проверены) | **PASS** |
| **R** | Каталог товаров успешно опрашивается и отдаёт данные | **PASS** |
| **S** | Детальная карточка товара загружается с фото | **PASS** |
| **T** | Раздел отчётов и финансовый леджер загружаются | **PASS** |
| **U** | Журнал ремонтов загружается (66 заказов) | **PASS** |
| **V** | Модуль расширения Авито загружает категории | **PASS** |
| **W** | Раздел управления бэкапами доступен Владельцу | **PASS** |
| **X** | Восстановление не затронуло текущие рабочие данные `data/` | **PASS** |
| **Y** | Разделение политик: веб-восстановление сохраняет живой auth | **PASS** |
| **Z** | Все сопутствующие регрессионные тесты завершены успешно | **PASS** |

---

## 5. Изоляция при тестировании

Все тестовые прогоны аварийного восстановления запускаются в изолированных временных каталогах операционной системы (`tempfile.mkdtemp` / `tmp_path`), что на 100% защищает рабочую локальную базу данных `C:\tbootit\data\db\technoreboot.db` и действующий PKI от любых модификаций (проверено побайтовым хешированием SHA256 до и после тестов).

---

## 6. Подтверждение на реально работающем изолированном Docker-стеке (Stage 07E-R1-R1)

Для исключения любых сомнений в работоспособности восстановленной системы реализовано сквозное подтверждение на **полноценно запущенном втором Docker-стеке**:

1. **Параллельный запуск на альтернативных портах:**
   - Шлюз mTLS: `9443:8443` (вместо живого `8443`)
   - Core API: `9000:8000` (вместо живого `8000`)
   - Admin-Shell: `9011:8010` (вместо живого `8011`)
   - Avito Module: `9020`, `9061`
   - Inventory Module: `9030`
   - Repairs Module: `9040`
   - Отдельный проект: `technoreboot-recovery-<run-id>`
   - Отдельные имена контейнеров: `technoreboot-recovery-<service>-<run-id>`

2. **Неопровержимое различие между живым контуром и восстановленным:**
   - **Живой рабочий контур (порт 8443):** 50 товаров (результат свежего импорта первой страницы после сброса каталога).
   - **Восстановленный контур (порт 9443):** 227 товаров из резервной копии, 50 продаж, 66 ремонтов, 490 фотографий.
   - Запросы к `https://127.0.0.1:9443/inventory/products` отдают полный каталог из 227 товаров с фотографиями.

3. **Собственный контур mTLS:**
   - Шлюз 9443 монтирует сертификаты и CA исключительно из изолированной директории `.recovery-test/<run-id>/data/auth`.
   - Запросы с сертификатом Владельца возвращают HTTP 200 OK на всех закрытых маршрутах (`/backups`, `/certificates`).
   - Запросы без сертификата отклоняются шлюзом (HTTP 403 Forbidden).

4. **Гарантия нетронутости боевого локального стека:**
   - SHA256 живой базы данных `data/db/technoreboot.db` до и после теста совпадает.
   - SHA256 живого CA `data/auth/ca/ca.crt` до и после теста совпадает.
   - Число перезапусков живых контейнеров: **0** (контейнеры не останавливались и не перезапускались).

---

## 7. Доказательство свежей сборки Git и побайтовой целостности бэкапа (Stage 07E-R1-R2)

На этапе Stage 07E-R1-R2 было выполнено строгое закрытие двух пробелов (Gap 1 и Gap 2), гарантирующее полную автономность и целостность:

### 1. Закрытие Пробела 1 (Gap 1: Доказательство сборки из исходников с нуля):
- Склонирован свежий репозиторий из GitHub `origin/main` (`https://github.com/apc90210/tbootit.git`) в изолированный каталог `.recovery-test/<run-id>/repo`.
- Проверена чистота рабочего дерева (`git status --porcelain` пуст, `fresh_clone: true`).
- Собраны уникальные Docker-образы из исходников со специфическими для запуска тегами:
  - `technoreboot-recovery-<run-id>-core`
  - `technoreboot-recovery-<run-id>-gateway`
  - `technoreboot-recovery-<run-id>-admin-shell`
  - `technoreboot-recovery-<run-id>-avito-module`
  - `technoreboot-recovery-<run-id>-inventory-sales-module`
  - `technoreboot-recovery-<run-id>-repairs-module`
- Проверено, что ID собранных образов являются полностью новыми и ни один локальный образ рабочего контура (`technoreboot-core:latest` и др.) **не был переиспользован** (`live_image_ids_used: false`).
- Устранена несовместимость окончаний строк Windows CRLF в Linux контейнерах (`.gitattributes` и `sed -i 's/\r$//' /entrypoint.sh`).

### 2. Закрытие Пробела 2 (Gap 2: Сплошная побайтовая проверка целостности дерева файлов):
- Произведено сплошное хеширование SHA256 каждого файла в архиве `TECHNOREBOOT_BACKUP_2026-09-11_103952.zip` и в восстановленной директории (без сэмплирования):
  - **Storage (`storage/`):** 1236 файлов, 9 750 814 байт, SHA256 дерева `65ec8a3d9591a8a6c379b833520aa4884eb55b1a282400d294f27e9f59602d7f` (0 пропущенных, 0 лишних, 0 расхождений хеша).
  - **Auth (`auth/`):** 54 файла, SHA256 дерева `1a0893765b3b43230744e68be6932e7b913bed2febbe28e50cd2aeaed3f97a7d` (0 пропущенных, 0 расхождений).
  - **Avito (`avito-module/`):** 812 файлов, SHA256 дерева `29cd969b6dc2b3df72e406c11762fc0cc1818f89988ffd790a47721db2ef0b79` (0 пропущенных, 0 расхождений).

### 3. Разрешение исторического расхождения 1236 vs 1214:
- Проведено детерминированное математическое доказательство:
  - В корне каталога `storage/product_photos/` находится ровно **1214 файлов**.
  - В 9 подкаталогах (`24/`, `69/`, `163/`, `164/`, `165/`, `166/`, `167/`, `168/`, `169/`) находится ещё ровно **22 файла** (21 `.jpg`, 1 `.webp`).
  - `1214 + 22 = 1236`.
  - Прежнее число 1214 возникало из-за нерекурсивного поиска файлов `glob("*.jpg")` в корне, тогда как рекурсивный обход `rglob("*")` находит все 1236 файлов (1235 `.jpg` и 1 `.webp`).

### 4. Референциальный аудит База Данных <-> Медиа:
- Всего записей в таблице `product_photos`: **490**.
- Из них локальных ссылок (`storage_path IS NOT NULL`): **475**.
  - Все 475 локальных ссылок существуют на диске в `storage/product_photos/` (0 отсутствующих).
  - Ни один из 475 файлов не имеет нулевой размер (0 zero-byte файлов).
- Из них внешних ссылок (`storage_path IS NULL`, превью Авито `avito_url`): **15**.
- Оставшиеся файлы на диске: `1236 - 475 = 761` сохранённый исторический медиафайл (сироты сохранены в бэкапе для предотвращения потери данных при ревизиях товаров).

### 5. Результаты автоматизированного тестирования (A - Z):
- Все 26 тестов набора Stage 07E-R1-R2 (`tests/test_stage07e_r1_r2_fresh_build_integrity.py`) успешно пройдены (**PASS**).
- Живой контур на порту 8443 изолирован, не перезапускался и функционирует штатно.


