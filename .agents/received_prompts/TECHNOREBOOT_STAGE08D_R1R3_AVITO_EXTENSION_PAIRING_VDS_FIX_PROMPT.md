# TECHNOREBOOT — Stage 08D-R1R3
## Avito Extension pairing fix on VDS/IP production

**Project:** ТехноРебут  
**Local workspace:** `C:\tbootit`  
**Production VDS:** `144.31.50.134`  
**Production URL:** `https://144.31.50.134`  
**Stage:** `Stage 08D-R1R3 — Fix Avito Extension Pairing on VDS`

# 0. INCIDENT

Owner manual browser check found a real production bug.

On VDS, Avito Extension pairing fails with:

```text
Ошибка сервера 400: Код подключения не найден.
```

This is a REAL Owner-browser failure and overrides previous automated acceptance.

Important inference:
- extension is reaching a server and receiving HTTP 400;
- therefore do NOT assume this is just a localhost/host-permission problem;
- reproduce and trace the exact pairing-code lifecycle end-to-end.

Do NOT touch or repopulate VDS business data.
Do NOT copy local DB/media/auth/Avito business state to VDS.
Do NOT reset mTLS/CA/OWNER/USER.
Do NOT wipe production data.

First copy this prompt unchanged from:

`C:\Users\Apc\Downloads\TECHNOREBOOT_STAGE08D_R1R3_AVITO_EXTENSION_PAIRING_VDS_FIX_PROMPT.md`

to:

`C:\tbootit\.agents\received_prompts\TECHNOREBOOT_STAGE08D_R1R3_AVITO_EXTENSION_PAIRING_VDS_FIX_PROMPT.md`

---

# 1. PREFLIGHT

Record:

```text
LOCAL_HEAD
ORIGIN_MAIN_HEAD
LOCAL_GIT_STATUS
LOCAL_STACK_RUNNING
VDS_STACK_RUNNING
VDS_REPO_HEAD
VDS_PRODUCTS
VDS_SALES
VDS_REPAIRS
VDS_PHOTOS
VDS_EXTERNAL_LISTINGS
VDS_DB_SHA256
VDS_CA_SHA256
```

Expected production business state remains clean:

```text
PRODUCTS=0
SALES=0
REPAIRS=0
PHOTOS=0
EXTERNAL_LISTINGS=0
```

If production business data is no longer zero because real users have already begun entering data, DO NOT delete it. Record current counts and preserve them.

---

# 2. REPRODUCE THE FAILURE EXACTLY

Using the production route:

```text
https://144.31.50.134/avito/extension
```

Trace the actual pairing flow:

1. generate/show a fresh 6-digit pairing code from the production page;
2. identify the exact backend endpoint called to create/store the code;
3. identify the exact endpoint called by the Chrome Extension to redeem/validate the code;
4. record request method/path/status;
5. reproduce the same HTTP 400:
   ```text
   Код подключения не найден
   ```
6. inspect VDS server logs around the failed request.

Do not fix anything until the exact failure is reproduced.

---

# 3. TRACE PAIRING STATE STORAGE

Determine exactly where pairing codes are stored.

Inspect:
- `admin-shell`;
- `avito-module`;
- production compose/env;
- persistent VDS Avito state under `/srv/technoreboot/data`;
- any in-memory dictionaries/caches;
- any files such as pairing/session JSON;
- any DB tables used for pairing.

For the pairing code lifecycle prove:

```text
PAIRING_CODE_CREATED_AT:
PAIRING_CODE_STORAGE_BACKEND:
PAIRING_CODE_STORAGE_PATH_OR_TABLE:
PAIRING_CODE_VISIBLE_TO_CREATOR_PROCESS:
PAIRING_CODE_VISIBLE_TO_REDEEM_PROCESS:
PAIRING_CODE_TTL:
PAIRING_CODE_ONE_TIME_USE:
```

Check specifically for these failure classes:

- code created in one container but redeemed in another;
- code stored only in process memory and lost/restarted;
- code stored in a local container filesystem instead of persistent VDS volume;
- code generated by admin-shell but validation performed by avito-module with separate state;
- code state removed by the Stage08D clean-reset;
- code path still references local/dev storage;
- production env points pairing create/redeem endpoints at different base URLs;
- code normalization bug (string/int, leading zero, whitespace);
- pairing code already consumed/expired;
- code-only deploy recreates containers and discards pairing state.

---

# 4. VERIFY PRODUCTION ORIGIN IN EXTENSION

Audit the actual current extension package served by production.

Verify:
- current extension version;
- manifest host permissions;
- API base URL / pairing base URL;
- no required hardcoded `localhost`;
- no required hardcoded `127.0.0.1`;
- production target is `https://144.31.50.134` or is dynamically supplied by the pairing package/page;
- mTLS/browser certificate behavior is compatible with extension fetches.

Do NOT change host configuration unless evidence shows it is required.

---

# 5. CORRECT PAIRING ARCHITECTURE

Implement the smallest robust fix.

Required architecture:

- pairing code generation and redemption must use ONE canonical persistent state;
- state must survive container recreation/code-only deployment for the intended TTL;
- state must be VDS-local only;
- local DEV pairing state must remain independent;
- pairing state must NOT be mixed with product/listing/business data;
- production business reset must not accidentally destroy required extension infrastructure state;
- pairing codes may expire and be one-time use, but a freshly generated code must redeem successfully.

Preferred:
- persistent file/state under a dedicated VDS path such as:
  `/srv/technoreboot/data/avito-module/pairing/`
  or the project’s existing canonical equivalent;
- atomic writes;
- clear TTL;
- no secrets in Git.

If pairing codes are intentionally ephemeral and should NOT survive restart, that is acceptable only if generation and redemption are guaranteed to hit the same canonical service/state. Document it.

---

# 6. KEEP BUSINESS DATA SAFE

Before any VDS deployment:
- create production backup using current code-only deploy mechanism;
- record current business counts;
- record DB SHA256 / CA SHA256.

After deployment:
- verify all business counts unchanged;
- verify no local DB/media copied to VDS;
- verify CA unchanged.

This stage must NOT use `bootstrap_restore.py`.

---

# 7. TESTS

Add focused tests for pairing:

```text
fresh code -> redeem = success
unknown code -> 400
expired code -> rejected
used code -> rejected if one-time-use
leading-zero code -> works if 6-digit code may start with zero
container/service restart behavior -> matches documented persistence design
local and VDS pairing stores remain independent
```

If extension source changes:
- run all extension tests;
- bump extension version.

Required:

```text
FAILED = 0
```

---

# 8. DEPLOY CODE ONLY

Deploy to VDS using:

```text
deploy/production/update_code_only.sh
```

or the already accepted code-only path.

Do NOT sync production data from local.

Verify all 6 services healthy.

---

# 9. END-TO-END PRODUCTION PAIRING PROOF

On VDS:

1. open production extension page;
2. generate a new 6-digit code;
3. confirm the code exists in canonical pairing storage;
4. redeem exactly that code through the same HTTP/API path used by Chrome Extension;
5. require success;
6. verify extension receives/stores its production connection state;
7. verify a second unknown code still returns 400;
8. if one-time use: verify reused code is rejected for the correct reason.

Required:

```text
FRESH_PAIRING_CODE_CREATED = true
FRESH_PAIRING_CODE_REDEEMED = true
PAIRING_HTTP_STATUS = 200
UNKNOWN_CODE_HTTP_STATUS = 400
PRODUCTION_TARGET = https://144.31.50.134
```

Do NOT publish/import any Avito listing for this stage.

---

# 10. OWNER BROWSER CHECK PREPARATION

At the end provide exact manual steps only:

1. Open `https://144.31.50.134/avito/extension`.
2. Download/install/reload the current extension ZIP if version changed.
3. Generate a new 6-digit pairing code.
4. Enter that code in extension popup.
5. Expected: pairing succeeds; no “Код подключения не найден”.

Do not require CMD/PowerShell from Owner.

---

# 11. DOCUMENTATION

Create:

`reports/stage08d_r1r3_avito_extension_pairing_vds_fix_report.md`

Update:

`docs/production_status.md`

`docs/production_deployment_model.md` if pairing persistence rules need documentation.

Update:

`logs/2026-09-12.md`

Preserve received prompt.

---

# 12. GIT / SAFETY

Never commit:
- production env;
- private keys;
- pairing secrets/tokens;
- runtime pairing files;
- DB;
- media;
- backup ZIPs.

Commit only safe code/tests/docs/reports/logs.

Push `origin/main`.

Final worktree clean.

---

# 13. FINAL REPORT CONTRACT

Return:

```text
# Stage 08D-R1R3 — Avito Extension Pairing VDS Fix

## Reproduction
PRODUCTION_URL:
PAIRING_PAGE:
PAIRING_CREATE_ENDPOINT:
PAIRING_REDEEM_ENDPOINT:
ORIGINAL_FAILURE_REPRODUCED:
ORIGINAL_HTTP_STATUS:
ORIGINAL_MESSAGE:

## Root Cause
ROOT_CAUSE:
PAIRING_STORAGE_BACKEND:
PAIRING_STORAGE_PATH_OR_TABLE:
WAS_PROCESS_MEMORY_ONLY:
WAS_CONTAINER_LOCAL_ONLY:
WAS_RESET_BY_CLEANUP:
CREATE_REDEEM_STATE_MISMATCH:
HOST_TARGET_ISSUE:

## Fix
FIX_SUMMARY:
PAIRING_STATE_PERSISTENCE:
PAIRING_TTL:
ONE_TIME_USE:
EXTENSION_VERSION_BEFORE:
EXTENSION_VERSION_AFTER:
PRODUCTION_TARGET:

## Tests
PAIRING_TESTS:
EXTENSION_TESTS:
FAILED:

## Production Safety
PRE_DEPLOY_BACKUP:
PRODUCTS_BEFORE:
PRODUCTS_AFTER:
SALES_BEFORE:
SALES_AFTER:
REPAIRS_BEFORE:
REPAIRS_AFTER:
PHOTOS_BEFORE:
PHOTOS_AFTER:
EXTERNAL_LISTINGS_BEFORE:
EXTERNAL_LISTINGS_AFTER:
CLIENT_CA_SHA256_UNCHANGED:
LOCAL_DATA_SYNCED_TO_VDS: false

## Runtime
ALL_SERVICES_HEALTHY:
PAIRING_CODE_CREATED:
PAIRING_CODE_REDEEMED:
PAIRING_HTTP_STATUS:
UNKNOWN_CODE_HTTP_STATUS:

## Git
COMMIT:
PUSH:
HEAD_AFTER:
FINAL_GIT_STATUS:

FINAL_STATUS:
TECHNOREBOOT_STAGE08D_R1R3_AVITO_EXTENSION_PAIRING_VDS_FIXED

PRODUCTION_URL: https://144.31.50.134
BUSINESS_DATA_PRESERVED: true
FUTURE_DEPLOYS_CODE_ONLY: true
DO_NOT_START_NEXT_STAGE_WITHOUT_OWNER_ACCEPTANCE: true
```

Return BLOCKED if:
- fresh pairing code cannot be redeemed;
- business data changes unexpectedly;
- CA/auth changes;
- code-only deploy path is bypassed;
- tests fail.

---

# 14. STOP

After technical pairing proof:

STOP.

Wait for Owner manual browser acceptance.
